interface MessageBubbleProps {
  alignment?: any
  timestamp?: any
}

export default function MessageBubble({ alignment, timestamp }: MessageBubbleProps) {
  return (
    <div className="">
      {/* MessageBubble component content */}
    </div>
  )
}