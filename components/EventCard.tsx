interface EventCardProps {
  showDate?: any
  showLocation?: any
  showAttendees?: any
}

export default function EventCard({ showDate, showLocation, showAttendees }: EventCardProps) {
  return (
    <div className="">
      {/* EventCard component content */}
    </div>
  )
}